# WORKFLOW DESATIVADO
# Este arquivo foi substituído por siaol_ml_cron.yml (SIAOL-PRO v7)
# Mantido vazio para não gerar execuções fantasmas

name: "[DESATIVADO] SIAOL-PRO v6.0 Autonomous Cycle"

on:
  workflow_dispatch: # Apenas manual, sem cron

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow desativado. Use siaol_ml_cron.yml."
